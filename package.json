{
  "name": "solid-image",
  "version": "0.0.3",
  "description": "Use the Sharp Library to quickly create images and HTML code",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fr dist/* && tsc",
    "prepublish": "npm run build",
    "publish": "npm version patch && npm publish",
    "dry": "npm publish --dry-run"
  },
  "keywords": [],
  "author": "Bryon Smith",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.13.0",
    "@types/sharp": "^0.31.1"
  },
  "dependencies": {
    "sharp": "^0.31.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webmastersmith/solid-images"
  }
}
