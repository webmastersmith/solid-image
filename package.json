{
  "name": "solid-image",
  "version": "0.0.12",
  "description": "Use the Sharp Image Library to quickly create images and HTML code",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fr lib/* && tsc",
    "prepac": "npm run build",
    "pac": "npm version patch --no-git-tag-version && npm publish",
    "dry": "npm publish --dry-run"
  },
  "keywords": [
    "responsive",
    "image",
    "img",
    "picture",
    "resolution switching",
    "Art Direction"
  ],
  "author": "Bryon Smith",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.13.0",
    "@types/sharp": "^0.31.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "console-progress-bar": "^1.0.8",
    "sharp": "^0.31.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webmastersmith/solid-images"
  }
}
